<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "My Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
    <style>
      body { font-family: system-ui, -apple-system, sans-serif; max-width: 800px; margin: 0 auto; padding: 1rem; color: #333; }
      nav { border-bottom: 2px solid #eee; padding-bottom: 1rem; margin-bottom: 2rem; }
      nav a { margin-right: 1.5rem; text-decoration: none; color: #0066cc; font-weight: 500; }
      nav a:hover { text-decoration: underline; }
      .post-meta { color: #666; font-size: 0.9rem; margin-bottom: 0.5rem; }
      .post-list article { margin-bottom: 2rem; padding-bottom: 1.5rem; border-bottom: 1px solid #eee; }
      .post-list article:last-child { border-bottom: none; }
      .category-tag { display: inline-block; background: #e8f0fe; color: #1a73e8; padding: 0.2rem 0.6rem; border-radius: 3px; font-size: 0.85rem; text-decoration: none; margin-right: 0.5rem; }
      .category-tag:hover { background: #d2e3fc; }
      code { background: #f4f4f4; padding: 0.15rem 0.4rem; border-radius: 3px; font-size: 0.9em; }
      pre { background: #f4f4f4; padding: 1rem; border-radius: 4px; overflow-x: auto; }
      pre code { background: none; padding: 0; }
      footer { margin-top: 3rem; padding-top: 1rem; border-top: 2px solid #eee; color: #999; font-size: 0.85rem; }
    </style>
  </head>

  <body>
    <nav>
      <%= link_to "Home", root_path %>
      <%= link_to "About", page_path("about") %>
      <%= link_to "Categories", categories_path %>
    </nav>

    <%= yield %>

    <footer>
      <p>Built with Rails &amp; rails2static</p>
    </footer>
  </body>
</html>
